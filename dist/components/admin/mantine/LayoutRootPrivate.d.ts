import { Router } from '../../../common/types';
import React from 'react';
export declare const LayoutRootPrivate: (props: {
    router: Router;
    children: React.ReactNode;
}) => React.JSX.Element;
